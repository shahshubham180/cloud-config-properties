spring:
  datasource:
    username: ${{secrets.JDBC_DATABASE_USERNAME}}
    url: jdbc:mysql://mysqldb:3306/customerportallibrary
    password: ${{secrets.JDBC_DATABASE_PASSWORD}}
  jpa:
    generate-ddl: true

library-app:
  uri: https://localhost:8700/
    
senior:
  citizen: James
junior:
  citizen: Bond

auth:
  password: ${PASSWORD}
